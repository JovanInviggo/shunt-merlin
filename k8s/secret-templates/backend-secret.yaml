# TEMPLATE — never commit real values. Run the kubectl command below instead.
#
# For DEV:
#   kubectl create secret generic backend-secret \
#     --namespace shunt-wizzard-dev \
#     --from-literal=DATABASE_USER=postgres \
#     --from-literal=DATABASE_PASSWORD=dev-password \
#     --from-literal=JWT_SECRET=$(openssl rand -base64 64) \
#     --from-literal=S3_STORAGE_BUCKET_NAME=your-dev-bucket \
#     --from-literal=S3_STORAGE_ACCESS_KEY=your-access-key \
#     --from-literal=S3_STORAGE_SECRET_KEY=your-secret-key
#
# For PROD:
#   kubectl create secret generic backend-secret \
#     --namespace shunt-wizzard-prod \
#     --from-literal=DATABASE_USER=postgres \
#     --from-literal=DATABASE_PASSWORD=STRONG_PROD_PASSWORD \
#     --from-literal=JWT_SECRET=$(openssl rand -base64 64) \
#     --from-literal=S3_STORAGE_BUCKET_NAME=your-prod-bucket \
#     --from-literal=S3_STORAGE_ACCESS_KEY=your-access-key \
#     --from-literal=S3_STORAGE_SECRET_KEY=your-secret-key
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
type: Opaque
stringData:
  DATABASE_USER: REPLACE
  DATABASE_PASSWORD: REPLACE
  JWT_SECRET: REPLACE
  S3_STORAGE_BUCKET_NAME: REPLACE
  S3_STORAGE_ACCESS_KEY: REPLACE
  S3_STORAGE_SECRET_KEY: REPLACE
