# TEMPLATE — never commit real values. Run the kubectl command below instead.
#
# For DEV:
#   kubectl create secret generic postgres-secret \
#     --namespace shunt-wizzard-dev \
#     --from-literal=POSTGRES_USER=postgres \
#     --from-literal=POSTGRES_PASSWORD=dev-password \
#     --from-literal=POSTGRES_DB=shunt_wizzard
#
# For PROD:
#   kubectl create secret generic postgres-secret \
#     --namespace shunt-wizzard-prod \
#     --from-literal=POSTGRES_USER=postgres \
#     --from-literal=POSTGRES_PASSWORD=STRONG_PROD_PASSWORD \
#     --from-literal=POSTGRES_DB=shunt_wizzard
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
type: Opaque
stringData:
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: REPLACE_WITH_STRONG_PASSWORD
  POSTGRES_DB: shunt_wizzard
